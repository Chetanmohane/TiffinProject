/**
 * Supabase client is no longer used as the project has migrated to MongoDB.
 * This file is kept empty to prevent import errors in other files that might still reference it.
 */
export const supabase = null as any;
